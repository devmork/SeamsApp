# SEAMS API

ASP.NET Core 8 Web API for student applications, officers, events, and attendance logging.

**Full documentation:** [docs/README.md](docs/README.md)

```bash
dotnet restore
dotnet run
```

Swagger is available on the running host (see `Properties/launchSettings.json`).
